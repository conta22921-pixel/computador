name: Build Flet Android APK - Artifact Download Link

# --- SEÇÃO CRÍTICA (CORRIGIDA) ---
on:
  # Dispara o workflow em todo push para a branch 'main'
  push:
    branches:
      - main
      
  # Permite o disparo manual (sem argumentos)
  workflow_dispatch: 
  
# --- FIM DA SEÇÃO CRÍTICA ---

jobs:
# ... (restante do seu código) ...
